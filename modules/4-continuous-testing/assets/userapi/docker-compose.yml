version: '3'

services:
  redis:
    image: "redis"
    restart: always
  web:
    build: .
    ports:
      - "5000:8080"
    environment:
      REDIS_HOST: 'redis'
      REDIS_PORT: 6379
    depends_on:
      - redis
    volumes: 
      - ./redis-vol:/data